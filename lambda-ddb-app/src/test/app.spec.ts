import 'mocha';
//import * as app from '../app';
import { expect } from 'chai';

describe('App.ts Test suite', function() {
    it('Application Test Suite', async() => {
        
        const array = new Array<PaymentItem>();
        
        expect(true).to.equal(true);
    });
});